<Page
    x:Class="Quoridor.UI.LogInPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quoridor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Name="Status" FontSize="8"/>
            <TextBox Name="UsernameTextBox" PlaceholderText="Username" TextWrapping="Wrap" Width="270"/>
            <PasswordBox Name="PasswordBox" PlaceholderText="Password" Margin="0,10,0,0"  Width="270"/>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0" HorizontalAlignment="Center">
                <Button Name="LogInBtn" Content="LogIn" Click="Button_Click"/>
                <Button Name="RegisterBtn" Content="Register" Click="Register_Click" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
